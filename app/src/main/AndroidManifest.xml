<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fastlk_redesign_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LookingForSomethingToBuy"></activity>
        <activity android:name=".DogDetails" />
        <activity android:name=".AllAnimalsDetails" />
        <activity android:name=".SelectAdTypeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ForgotPwdActivity" />
        <activity android:name=".SellActivity" />
        <activity android:name=".LocCatActivity" />
        <activity android:name=".AdsViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>